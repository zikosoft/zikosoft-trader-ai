# Squelette (B01-B04) — configuration mono-instance minimale (stockage
# filesystem local), suffisante pour que le conteneur démarre dès le jour 1.
# À revoir en B24 si un besoin de rétention/scalabilité plus poussé apparaît.
auth_enabled: false

server:
  http_listen_port: 3100

common:
  path_prefix: /loki
  storage:
    filesystem:
      chunks_directory: /loki/chunks
      rules_directory: /loki/rules
  replication_factor: 1
  ring:
    instance_addr: 127.0.0.1
    kvstore:
      store: inmemory

schema_config:
  configs:
    - from: 2024-01-01
      store: tsdb
      object_store: filesystem
      schema: v13
      index:
        prefix: index_
        period: 24h

limits_config:
  allow_structured_metadata: true
